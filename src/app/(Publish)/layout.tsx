'use client'

import { PublshLayout } from '@/widgets'

export default function PublishLayout({
	children,
}: Readonly<{
	children: React.ReactNode
}>) {
	return <PublshLayout>{children}</PublshLayout>
}
